# Vercel's Python builder (@vercel/python) reads requirements.txt from the
# repo root, not pyproject.toml. Kept in sync with pyproject.toml manually —
# pyproject.toml stays the source of truth for local dev (pip install -e .).
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0
anthropic==0.121.0
httpx==0.28.1
python-dotenv==1.2.2
apscheduler==3.11.3
upstash-redis>=1.1.0
# Multi-usuario (2026-09-09): email+password + Google-login JWT auth.
python-jose[cryptography]==3.5.0
bcrypt==4.2.1
google-auth==2.35.0
pydantic[email]==2.13.4
# NOTE: playwright is intentionally NOT listed here — this file is read by
# Vercel's production build (@vercel/python) and playwright is a test-only
# dependency (qa_e2e_runner.py). It's declared under pyproject.toml's
# [project.optional-dependencies] "test" extra instead — see that file and
# PLAN-qa-automation.md.
