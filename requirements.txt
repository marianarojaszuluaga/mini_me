# Vercel's Python builder (@vercel/python) reads requirements.txt from the
# repo root, not pyproject.toml. Kept in sync with pyproject.toml manually —
# pyproject.toml stays the source of truth for local dev (pip install -e .).
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0
anthropic==0.121.0
httpx==0.28.1
python-dotenv==1.2.2
apscheduler==3.11.3
upstash-redis>=1.1.0
